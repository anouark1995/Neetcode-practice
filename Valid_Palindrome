class Solution:
    def isPalindrome(self, s: str) -> bool:
        s_alphanumonly = (''.join(c for c in s if c.isalnum())).lower()
        return s_alphanumonly == s_alphanumonly[::-1]
        


solution = Solution()
print(solution.isPalindrome("Was  a car or a cat I saw?"))